vars:
  city: Moscow

files:
  - template_path: user.tmpl
    output_path: 'user_{{ vars.local.user.id }}.yaml'
    vars:
      user:
        id: $USER_ID
        name: "John"
        phone_number: 1234
